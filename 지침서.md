# 고등학교 수학 시각화 프로젝트 지침서

## 프로젝트 목적
고등학교 수학 교과서 문제를 Streamlit + Matplotlib으로 시각화하여,
학생들이 개념을 직관적으로 이해할 수 있도록 돕는다.

---

## 기술 스택
- **Python** 3.x
- **Streamlit** — UI 및 배포
- **Matplotlib** — 그래프
- **NumPy** — 수치 계산
- **배포** — Streamlit Cloud (share.streamlit.io)

---

## 폴더 구조
```
math-visualizer/
├── requirements.txt        ← 공통 패키지
├── 01_접선의_개수.py
├── 02_방정식의_실근.py
└── 03_다음_문제.py         ← 번호 붙여서 추가
```

> 앱이 많아지면 Streamlit 멀티페이지(pages/ 폴더) 구조로 전환

---

## requirements.txt
```
streamlit
matplotlib
numpy
```

---

## 앱 파일 구조 템플릿

각 앱은 아래 순서로 구성한다.

```python
import streamlit as st
import numpy as np
import matplotlib.pyplot as plt

# 1. 폰트 설정 (항상 포함)
plt.rcParams['axes.unicode_minus'] = False

# 2. 페이지 설정
st.set_page_config(page_title="앱 제목", layout="centered")

# 3. 제목 및 문제 설명 (한글 가능 - Streamlit UI)
st.title("문제 제목")
st.markdown("문제 설명 및 수식")

# 4. 슬라이더 (핵심 변수)
k = st.slider("변수명", min_value=..., max_value=..., value=..., step=0.1)

# 5. 수학적 계산
# ...

# 6. 결과 표시 (success / warning / error)
if 조건:
    st.success("결과 메시지")

# 7. 그래프 (Matplotlib - 내부 텍스트는 영문/수식만 사용)
fig, ax = plt.subplots(figsize=(8, 6))
# ...
ax.set_title(f'영문 제목', fontsize=13, pad=12)
st.pyplot(fig)

# 8. 풀이 보기 (접기/펼치기)
with st.expander("풀이 보기"):
    st.markdown(r"LaTeX 수식으로 풀이 작성")
```

---

## 그래프 스타일 가이드

| 항목 | 설정 |
|------|------|
| 그래프 크기 | `figsize=(8, 6)` |
| 곡선 색상 | `#185FA5` (파란색) |
| 접선/강조선 1 | `#D85A30` (주황색) |
| 접선/강조선 2 | `#1D9E75` (초록색) |
| 경계선/축 | `#aaaaaa` (회색) |
| 곡선 두께 | `linewidth=2.5` |
| 강조선 두께 | `linewidth=1.8`, `linestyle='--'` |
| 점 크기 | `markersize=7` |
| 그리드 색상 | `color='#eeeeee'`, `linewidth=0.5` |
| 위쪽/오른쪽 테두리 | `ax.spines['top/right'].set_visible(False)` |

---

## 주의사항

### 폰트
- **Streamlit UI** (제목, 슬라이더, 설명, 풀이): 한글 사용 가능
- **Matplotlib 그래프 내부** (제목, 레이블, 텍스트): **영문/수식만 사용**
  - 이유: Streamlit Cloud(Linux)에 한글 폰트 없음
  - 수식은 `r'$LaTeX$'` 형식으로 작성

### 배포
- Streamlit Cloud는 **Linux 환경** → 한글 폰트 없음
- `plt.rcParams['axes.unicode_minus'] = False` 항상 포함
- `requirements.txt` 는 레포지토리 루트에 하나만 유지

---

## 새 앱 추가 절차

1. 위 템플릿을 복사하여 `번호_앱이름.py` 로 저장
2. 로컬에서 테스트:
   ```powershell
   venv\Scripts\activate
   streamlit run 파일명.py
   ```
3. GitHub에 push:
   ```powershell
   git add 파일명.py
   git commit -m "add 문제설명"
   git push
   ```
4. Streamlit Cloud에서 새 앱 배포:
   - Repository: `math-visualizer`
   - Main file path: `파일명.py`
   - App URL: 원하는 이름 입력

---

## 지금까지 만든 앱

| 번호 | 파일명 | 문제 | URL |
|------|--------|------|-----|
| 01 | `tangent_app.py` | 점 (k,0)에서 y=(x-1)eˣ에 그은 접선의 개수 | - |
| 02 | `roots_app.py` | 방정식 x³+3/x=k의 실근 개수 | - |

> 배포 후 URL 칸을 채워서 관리
